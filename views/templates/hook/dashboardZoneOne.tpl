{if empty($changeFilesDone)}
    <div class="alert alert-danger">
        بومی سازی به صورت ناقص انجام شده است ، به پیکربندی ماژول بومی ساز مراجعه کنید:

        <a href="{$nativeLink}" target="_blank">
            تنظیمات بومی ساز
        </a>
    </div>
{/if}