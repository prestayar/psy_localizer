# ساختار داده

این ماژول در تغییرات فعلی جدول یا Migration اختصاصی ندارد. وضعیت تنظیمات با کلیدهای Configuration خود PrestaShop نگه‌داری می‌شود.

کلید مهم فعلی `Localizer_Native_Active` است که وضعیت فعال بودن قابلیت‌های Native ماژول را کنترل می‌کند.
