# ساختار داده

این ماژول در تغییرات فعلی جدول یا Migration اختصاصی ندارد. وضعیت تنظیمات با کلیدهای Configuration خود PrestaShop نگه‌داری می‌شود.

کلید مهم فعلی `Localizer_Native_Active` است که برای نمایش وضعیت فعال بودن قابلیت Native در checklist داشبورد خوانده می‌شود.
